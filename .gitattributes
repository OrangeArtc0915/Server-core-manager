# 换行符规则：固定下来，避免不同机器上 core.autocrlf 设置不同导致行为不一致
#
# 批处理必须是 CRLF：cmd.exe 对 LF 换行的批处理解析不稳，
# 尤其是 if (...) else (...) 这种多行块，可能被拆错。
*.bat  text eol=crlf
*.cmd  text eol=crlf
*.ps1  text eol=crlf

# 文档与数据用 LF，跨平台 diff 干净，GitHub 网页端也正常
*.md    text eol=lf
*.json  text eol=lf
LICENSE text eol=lf
.gitignore text eol=lf

# 图片按二进制处理，别做任何换行转换
*.png binary
